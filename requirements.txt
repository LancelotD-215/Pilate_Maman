# Requirements pour l'application de gestion des clients Pilates
# Mis à jour le 2026-08-14 (Python 3.12)

# Framework web principal
Flask==3.1.3

# Chargement des variables d'environnement depuis un fichier .env
python-dotenv==1.2.2

# Gestion des fuseaux horaires (timezone Paris)
pytz==2026.3.post1

# Lecture de fichiers Excel (utilisé par import_synergie.py)
openpyxl==3.1.5

# Note: Les modules sqlite3, datetime et os font partie
# de la bibliothèque standard Python et n'ont pas besoin d'être installés.
# Werkzeug, Jinja2, MarkupSafe, itsdangerous, click, blinker sont installés
# automatiquement comme dépendances transitives de Flask.
